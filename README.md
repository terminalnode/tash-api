# TASH API
An API created for a school project. The API should allow administrators to send TODOs to
operators in the field. It keeps track of users last pinged location in order to allow tasks
to be sent to the nearest operator.
